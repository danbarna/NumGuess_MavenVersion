package org.fasttrackit.dev.lesson1.numgenerator;

import java.util.ArrayList;
import java.util.List;

/**
 * Created by user376 on 15.08.2015.
 */
public class MyList {
   /* private static MyList m;

    public static MyList getInstance() {

        if(m==null) {
            m=new MyList();
        }
        return m;
    }

    List ll = new ArrayList<>();

    public double addItem(double value) {
        ll.add(value);
    }
    public getMin() {
        double min=100;
        for (int i:ll ) {
            if ()<min) {
                min=element.getScore();
                id=element.getId();
            }
        }
        printList();
        return new HallOfFame(id,min);
    }*/
}
